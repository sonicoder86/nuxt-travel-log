name: Lint

on:
  pull_request:
    branches:
      - main

jobs:
  build:

    runs-on: ubuntu-latest
    environment: Build
    env:
      NODE_ENV: development
      TURSO_DATABASE_URL: "http://127.0.0.1:8080"
      TURSO_AUTH_TOKEN: ""
      BETTER_AUTH_SECRET: 5Th45dzzzUYENwTXQTQTAPCoQLZgYpYI
      BETTER_AUTH_URL: "http://localhost:3000"
      GITHUB_CLIENT_ID: "Ov23lim"
      GITHUB_CLIENT_SECRET: "client-secret"
    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
      - run: npm ci
      - run: npm run lint
